"use strict" //treat all js code as newer version
let name="xyz";
var notuse; //var can be accessed and modified outside scope therefore not use
console.log(typeof name) //string
console.log(typeof null) //object
